<UserControl x:Class="UartMonitor.MyToolWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:imaging="clr-namespace:Microsoft.VisualStudio.Imaging;assembly=Microsoft.VisualStudio.Imaging"
             xmlns:theming="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Imaging"
             xmlns:util="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Utilities"
             xmlns:catalog="clr-namespace:Microsoft.VisualStudio.Imaging;assembly=Microsoft.VisualStudio.ImageCatalog"
             xmlns:toolkit="clr-namespace:Community.VisualStudio.Toolkit;assembly=Community.VisualStudio.Toolkit"
             toolkit:Themes.UseVsTheme="True"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="300"
             Name="MyToolWindow">
    <DockPanel Margin="8">

        <StackPanel DockPanel.Dock="Top" Orientation="Vertical">
            <WrapPanel Margin="0,0,0,6">

                <TextBlock Text="Port:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <ComboBox x:Name="PortComboBox" Width="100" Margin="0,0,8,0"/>

                <TextBlock Text="Baud:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <ComboBox x:Name="BaudComboBox" Width="100" Margin="0,0,8,0"/>

                <Button x:Name="RefreshButton" Content="Refresh" Width="75" Margin="0,0,6,0" Click="RefreshButton_Click"/>
                <Button x:Name="ConnectButton" Content="Connect" Width="80" Margin="0,0,6,0" Click="ConnectButton_Click"/>
                <Button x:Name="DisconnectButton" Content="Disconnect" Width="90" Margin="0,0,6,0" Click="DisconnectButton_Click"/>
                <Button x:Name="ClearButton" Content="Clear" Width="70" Click="ClearButton_Click"/>

                <CheckBox x:Name="AutoScrollCheckBox" Content="Auto-scroll" IsChecked="True" Margin="12,0,0,0" VerticalAlignment="Center"/>
            </WrapPanel>

            <TextBlock x:Name="StatusTextBlock" Text="Disconnected" Margin="0,0,0,6"/>
        </StackPanel>

        <RichTextBox x:Name="LogBox"
                     IsReadOnly="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas"
                     FontSize="12"
                     AcceptsTab="True"/>
    </DockPanel>
</UserControl>
